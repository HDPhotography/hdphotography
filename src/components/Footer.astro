<footer class="bg-gray-900 text-white py-12">
  <div class="container mx-auto px-6">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      <!-- Contact Info -->
      <div>
        <h3 class="text-xl font-display mb-4">Contact Us</h3>
        <p class="mb-2">📍 123 Photography Lane</p>
        <p class="mb-2">📱 (555) 123-4567</p>
        <p>📧 info@lensnlight.com</p>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-xl font-display mb-4">Quick Links</h3>
        <ul class="space-y-2">
          <li>
            <a 
            href="/" 
            class="hover:text-primary-500"
            onclick="event.preventDefault(); location.href='/'">
            Home
            </a>
          </li>
          <li>
            <a 
            href="/about" 
            class="hover:text-primary-500"
            onclick="event.preventDefault(); location.href='/about'">
            About
            </a>
          </li>
          <li>
            <a 
            href="/showroom" 
            class="hover:text-primary-500"
            onclick="event.preventDefault(); location.href='/showroom'">
            Showroom
            </a>
          </li>
        </ul>
      </div>

      <!-- Contact Form -->
      <div>
        <h3 class="text-xl font-display mb-4">Get in Touch</h3>
        <form class="space-y-4" id="contactForm">
          <input
            type="email"
            placeholder="Your Email"
            class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 focus:outline-none focus:border-primary-500"
            id="emailID"
            required
          />
          <textarea
            placeholder="Your Message"
            rows="4"
            class="w-full px-4 py-2 rounded bg-gray-800 border border-gray-700 focus:outline-none focus:border-primary-500"
            id="messageID"
            required
          ></textarea>
          <button
            type="submit"
            class="w-full bg-primary-500 text-white py-2 px-4 rounded hover:bg-primary-600 transition duration-300"
          >
            Send Message
          </button>
        </form>
      </div>
    </div>

    <div class="mt-12 pt-8 border-t border-gray-800 text-center">
      <p>&copy; 2024 Lens & Light Photography. All rights reserved.</p>
    </div>
  </div>
</footer>

<script>
  document.getElementById('contactForm').addEventListener('submit', function(e) { 
    e.preventDefault(); 
    const emailInput = document.getElementById('emailID') as HTMLInputElement; 
    const messageInput = document.getElementById('messageID') as HTMLTextAreaElement; 
    const senderEmail = emailInput.value; 
    const message = messageInput.value; 
    const receiverEmail = 'joe@main.joe'; 
    const subject = encodeURIComponent('Get ou-'); 
    const body = encodeURIComponent(message); 
    const mailtoLink = `mailto:${receiverEmail}?subject=${subject}&body=${body}&cc=${senderEmail}`;
    window.open(mailtoLink, '_blank'); 
  }); 
</script>


