<footer class="bg-[#f5f0e6] text-[#3d2e1e] py-16">
  <div class="container mx-auto px-6">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      <!-- Contact Info -->
      <div>
        <h3 class="text-xl font-display mb-4 text-[#b0904a]">Contact Us</h3>
        <p class="mb-2">📍 123 Photography Lane</p>
        <p class="mb-2">📱 (555) 123-4567</p>
        <p>📧 info@lensnlight.com</p>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-xl font-display mb-4 text-[#b0904a]">Quick Links</h3>
        <ul class="space-y-2">
          <li><a href="/" class="hover:text-[#c4a65d] transition">Home</a></li>
          <li><a href="/about" class="hover:text-[#c4a65d] transition">About</a></li>
          <li><a href="/showroom" class="hover:text-[#c4a65d] transition">Showroom</a></li>
        </ul>
      </div>

      <!-- Contact Form -->
      <div>
        <h3 class="text-xl font-display mb-4 text-[#b0904a]">Get in Touch</h3>
        <form class="space-y-4" id="contactForm">
          <input
            type="email"
            placeholder="Your Email"
            class="w-full px-4 py-2 rounded bg-[#fffaf2] border border-[#e0d3b8] placeholder-[#998d7c] text-[#3d2e1e] focus:outline-none focus:ring-2 focus:ring-[#c4a65d]"
            id="emailID"
            required
          />
          <textarea
            placeholder="Your Message"
            rows="4"
            class="w-full px-4 py-2 rounded bg-[#fffaf2] border border-[#e0d3b8] placeholder-[#998d7c] text-[#3d2e1e] focus:outline-none focus:ring-2 focus:ring-[#c4a65d]"
            id="messageID"
            required
          ></textarea>
          <button
            type="submit"
            class="w-full bg-[#c4a65d] text-white py-2 px-4 rounded-full font-semibold hover:bg-[#b0904a] transition duration-300"
          >
            Send Message
          </button>
        </form>
      </div>
    </div>

    <div class="mt-16 pt-8 border-t border-[#e0d3b8] text-center text-sm text-[#7b6a4d]">
      <p>&copy; 2024 Lens & Light Photography. All rights reserved.</p>
    </div>
  </div>
</footer>

<script>
  document.getElementById('contactForm').addEventListener('submit', function(e) { 
    e.preventDefault(); 
    const emailInput = document.getElementById('emailID') as HTMLInputElement; 
    const messageInput = document.getElementById('messageID') as HTMLTextAreaElement; 
    const senderEmail = emailInput.value; 
    const message = messageInput.value; 
    const receiverEmail = 'joe@main.joe'; 
    const subject = encodeURIComponent('Get ou-'); 
    const body = encodeURIComponent(message); 
    const mailtoLink = `mailto:${receiverEmail}?subject=${subject}&body=${body}&cc=${senderEmail}`;
    window.open(mailtoLink, '_blank'); 
  }); 
</script>


