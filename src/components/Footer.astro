<footer class="bg-cream dark:bg-gray-800 text-brown py-16">
  <div class="container mx-auto px-6">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      <!-- Contact Info -->
      <div>
        <h3 class="text-xl font-display mb-4 text-goldDark">Contact Us</h3>
        <p class="mb-2 dark:text-gray-300">📱 +91 9884245583</p>
        <p class="mb-2 dark:text-gray-300">📧 hdphotography.hdphoto@gmail.com</p>
        <p class="mb-2 dark:text-gray-300">📍 Near E. B, Avadi road, Poonamallee</p>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-xl font-display mb-4 text-goldDark">Social Media</h3>
        <ul class="space-y-2">
          <li><a href="https://www.instagram.com/__h.d_photography_" target="_blank" class="dark:text-gray-300">Instagram</a></li>
          <li><a href="https://www.facebook.com/share/16PSds7WBP/" target="_blank" class="dark:text-gray-300">Facebook</a></li>
          <li><a href="#" class="dark:text-gray-300">Twitter</a></li>
        </ul>
      </div>

      <!-- Contact Form -->
      <div>
        <h3 class="text-xl font-display mb-4 text-goldDark">Get in Touch</h3>
        <form class="space-y-4" id="contactForm">
          <input
            type="email"
            placeholder="Your Email"
            class="w-full px-4 py-2 rounded bg-ivory border border-champagne placeholder-oliveGray text-brown focus:outline-none focus:ring-2 focus:ring-goldLight"
            id="emailID"
            required
          />
          <textarea
            placeholder="Your Message"
            rows="4"
            class="w-full px-4 py-2 rounded bg-ivory border border-champagne placeholder-oliveGray text-brown focus:outline-none focus:ring-2 focus:ring-goldLight"
            id="messageID"
            required
          ></textarea>
          <button
            type="submit"
            class="w-full bg-goldLight text-white py-2 px-4 rounded-full font-semibold hover:bg-goldDark transition duration-300"
          >
            Send Message
          </button>
        </form>
      </div>
    </div>

    <div class="mt-16 pt-8 border-t border-champagne text-center text-sm text-brown dark:text-gray-300">
      <p>&copy; 2021 HD Photography. All rights reserved.</p>
    </div>
  </div>
</footer>

<script>
  document.getElementById('contactForm').addEventListener('submit', function(e) { 
    e.preventDefault(); 
    const emailInput = document.getElementById('emailID') as HTMLInputElement; 
    const messageInput = document.getElementById('messageID') as HTMLTextAreaElement; 
    const senderEmail = emailInput.value; 
    const message = messageInput.value; 
    const receiverEmail = 'hdphotography.hdphoto@gmail.com'; 
    const subject = encodeURIComponent("Hi HDPhotography, Let's work together!"); 
    const body = encodeURIComponent(message); 
    const mailtoLink = `mailto:${receiverEmail}?subject=${subject}&body=${body}&cc=${senderEmail}`;
    window.open(mailtoLink, '_blank'); 
  }); 
</script>


